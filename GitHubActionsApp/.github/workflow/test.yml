name: first workflow
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name : echo "Hey there"
          run: |
          node -v
          npm -v



